name: Testing, Checking & Formatting
on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
jobs:
  test:
    name: run tests
    runs-on: ubuntu-latest
    steps:
      - run: npm test

  lint:
    name: Check code
    runs-on: ubuntu-latest
    steps:
      - run: npm run lint

  pretty:
    name: Prettify
    runs-on: ubuntu-latest
    steps:
      - run: npm run format

